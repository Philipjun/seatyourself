<header class="navbar navbar-fixed-top center navbar-inverse">
    <div class="container">
      <%= link_to "Restaurants", '/restaurants', id: "logo" %>
      <nav>
        <ul class="nav pull-right">
        
          
        <p class="userinfo">
  <% if current_user %>
  <h4>Signed in as <%= current_user.email %></h4> <%= link_to "Log out", session_path("current"), :method => :delete %>
  <% else %>
  <%= link_to "Log in", new_session_path %> or <%= link_to "Sign up", new_user_path %>
  <% end %>
</p>
          <p><%= link_to "New Restaurant", new_restaurant_path %></p>
        </ul>
      </nav>
    </div>
  </div>
</header>